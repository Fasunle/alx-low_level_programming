#include "main.h"

/**
 * _strlen - prints string length
 * @s: pointer to string
 * Return: 0 for success or 1 for failure
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
